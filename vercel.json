{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd web && npm install --no-audit --no-fund && npm run build",
  "installCommand": "cd web && npm install --no-audit --no-fund",
  "outputDirectory": "web/.next"
}
