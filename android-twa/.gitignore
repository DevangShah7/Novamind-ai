# Build outputs and downloaded dependencies
app/build/
.gradle/
local.properties
*.iml
.idea/

# AAR/JAR dependencies (regenerate with download-deps.sh if needed)
androidx-*.aar
androidx-*.jar
browserhelper.aar

# Generated keystore (commit only if intentional; this is for dev/sideload testing)
debug.keystore

# Release keystore — never commit. Losing this means the app cannot be updated.
# Backup the .jks + password offline before first sideload.
release.jks
